<%= form_with model: [book,book_comment] do |f| %>
       <div class="form-group">
           <%= f.text_area :comment, class: "w-100" %>
       </div>
       <div class="form-group">
           <%= f.submit "送信" %>
       </div>
<% end %>